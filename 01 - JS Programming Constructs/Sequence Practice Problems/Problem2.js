// Problem 2: Use REPL – Use Random to get Dice Number between 1 to 6

// Generate a random number between 1 and 6 (inclusive) for a dice roll
let diceNumber = Math.floor(Math.random() * 6) + 1;
console.log("Random Dice Number:", diceNumber);