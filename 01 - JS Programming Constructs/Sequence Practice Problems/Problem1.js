// Problem 1: Use REPL – Random Function Math.floor(Math.random() * 10); to get a Single Digit

// Generate a random single digit (0-9) using Math.random()
let randomDigit = Math.floor(Math.random() * 10);
console.log("Random Single Digit:", randomDigit);